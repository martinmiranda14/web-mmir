---
interface Props {
  es: string;
  en: string;
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { es, en, as: Tag = 'span', class: className } = Astro.props;
---

<Tag data-es={es} data-en={en} class={className}>{es}</Tag>
